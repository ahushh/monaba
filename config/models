Group
    name             Text
    GroupUniqName    name
    permissions      [Permission]
    deriving         Typeable
    deriving         Eq
    deriving         Ord
    deriving         Show
User
    name             Text
    UserUniqName     name
    group            Text
    password         Text
    salt             Text
    deriving         Typeable
    deriving         Show
Board
    name              Text
    BoardUniqName     name
    description       Text
    longDescription   Text
    bumpLimit         Int
    numberFiles       Int
    allowedTypes      [String]
    defaultName       Text
    maxMsgLength      Int
    thumbSize         Int
    threadsPerPage    Int
    previewsPerThread Int
    hidden            Bool
    enableCaptcha     Bool
    threadLimit       Int      default=-1
    opFile            Text     -- Enabled/Disabled/Required
    replyFile         Text     -- Enabled/Disabled/Required
    viewAccess        [Text]   Maybe -- list of group names
    replyAccess       [Text]   Maybe -- list of group names
    threadAccess      [Text]   Maybe -- list of group names
    category          Text     Maybe
    opModeration      Bool     default=0
    extraRules        [Text]   
    enableGeoIp       Bool     default=0
    opEditing         Bool     default=0
    postEditing       Bool     default=0
    showEditHistory   Bool     default=0
    enableForcedAnon  Bool     default=0
    showPostDate      Bool     default=0
    deriving          Typeable
Post json
    board        Text
    localId      Int
    parent       Int
    owner        Text Maybe -- is not used yet, but might be useful in the future
    date         UTCTime
    bumped       UTCTime Maybe
    ip           Text
    country      GeoCountry Maybe
    sage         Bool
    sticked      Bool -- used for threads only
    locked       Bool -- used for threads only
    autosage     Bool -- used for threads only
    deleted      Bool default=0
    deletedByOp  Bool default=0
    posterId     Text 
    lastModified UTCTime Maybe default=null
    hellbanned   Bool default=0 -- visible only to post author
    -- form's values
    message      Textarea
    rawMessage   Text
    title        Text
    name         Text
    password     Text
    deriving     Typeable
    deriving     Eq
    deriving     Ord
    deriving     Show
History
    postId       PostId
    HistoryUniqPostId postId
    messages     [Textarea]
    dates        [UTCTime]
Attachedfile json
    parentId     PostId
    md5          Text
    name         FilePath              -- name of the saved file
    origName     FilePath              -- original file name
    type         String                -- file extension
    thumbSize    Int                   -- just thumbSize from Board table
    thumbWidth   Int      default=-1   -- real thumb width
    thumbHeight  Int      default=-1   -- real thumb height
    width        Int      default=-1   -- real image width
    height       Int      default=-1   -- real image height
    size         Text                  -- formatted file size
    rating       Text                  -- censorship rating
    deriving     Typeable
Ban
    ip           Text
    reason       Text
    expires      UTCTime Maybe
    board        Text Maybe
    deriving     Typeable
Captcha
    ip           Text
    localId      Int
    CaptchaUniqueLocalId localId
    info         Text             -- bolded/italic/regular - which characters to type
    value        Text
    expires      UTCTime
Config
    captchaLength   Int
    aCaptchaGuards  Int  -- number of captchas a user needs to solve in order to get the captcha disabled for him/her
    captchaTimeout  Int  -- in seconds
    replyDelay      Int  -- delay before posting in a thread
    threadDelay     Int  -- delay before createing a thread
    boardCategories [Text]
    newsBoard       Text -- a board news are show from
    showNews        Int  default=2  -- number of news shown on the home page
    maxEditings     Int  default=10 
    showRecentPosts Int  default=15 -- number of posts shown at /~/
    displaySage     Bool default=0
    modlogMaxEntries     Int default=3000
    modlogEntriesPerPage Int default=30
    about           Textarea
Hellban
    userId          Text 
    userIp          Text -- is not used yet
Modlog
    user            Text
    group           Text
    date            UTCTime
    message         Text
CaptchaDict
    word            Text
    lang            Text
