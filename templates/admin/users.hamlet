^{adminNavbarWidget}
^{formWidget}
<table .admin-table>
    <tr>
        <th>         
        <th>_{MsgName}
        <th>_{MsgGroup }
    $forall Entity _ user <- users
        <tr>
            <td>
                <a href=@{UsersDeleteR $ userName user}>_{MsgDeleteBtn}
            <td>#{userName user}
            <td>#{show $ userGroup user}
